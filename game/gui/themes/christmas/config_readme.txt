The first set EXACTLY matches the UI colors as defined in gui.rpy.

The second set is for supplemental colors added for custom screens such as Item Collection. If you add anything here, you have to add it to *all* the themes!

Color codes already include alpha, so you do not need to specify an alpha value (unless you really want/need to).